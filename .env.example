# Aigualba Environment Configuration
# Copy this file to .env and update values for your deployment
# Environment: production (set DASH_DEBUG=1 for development)
#
# IMPORTANT CONFIGURATION NOTES:
# - For production: Use HTTPS URLs with proper domain names
# - For development: Use http://localhost URLs with appropriate ports
# - All passwords marked with "change_me" MUST be updated for security
# - Keycloak admin console (auth.aigualba.cat) is restricted to local/private networks
# - Ensure DNS points both aigualba.cat and auth.aigualba.cat to your server

# Database Configuration
POSTGRES_USER=aigualba_user
POSTGRES_PASSWORD=change_me_in_production_secure_123!
POSTGRES_DB=aigualba

# Backend Configuration
DATABASE_URL=postgresql://aigualba_user:change_me_in_production_secure_123!@db:5432/aigualba

# Frontend Configuration  
BACKEND_URL=http://backend:8000

# Keycloak Configuration
# Production: Use HTTPS with auth.aigualba.cat subdomain
# Development: Use http://localhost:8080
KEYCLOAK_URL=https://auth.aigualba.cat
KEYCLOAK_REALM=aigualba
KEYCLOAK_CLIENT_ID=aigualba-frontend  
KEYCLOAK_CLIENT_SECRET=change_me_client_secret_production!
KEYCLOAK_REDIRECT_URI=https://aigualba.cat/admin
KEYCLOAK_ADMIN_PASSWORD=change_me_admin_password_123!

# Keycloak SSL Configuration (Production only)
# Update paths to match your SSL certificate location
KC_HTTPS_CERTIFICATE_FILE=/opt/keycloak/conf/certs/live/aigualba.cat/fullchain.pem
KC_HTTPS_CERTIFICATE_KEY_FILE=/opt/keycloak/conf/certs/live/aigualba.cat/privkey.pem

# Keycloak Database Configuration
KC_DB_USERNAME=keycloak_user
KC_DB_PASSWORD=change_me_keycloak_db_password_456!

# Application Configuration
DASH_DEBUG=0
ENVIRONMENT=production

# Development Environment Variables (uncomment for development)
# DEV_POSTGRES_USER=devuser
# DEV_POSTGRES_PASSWORD=devpass_dev_123!
# DEV_POSTGRES_DB=aigualba_db
# DEV_DATABASE_URL=postgresql://devuser:devpass_dev_123!@db:5432/aigualba_db